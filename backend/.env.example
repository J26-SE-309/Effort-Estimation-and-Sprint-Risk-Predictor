# Copy to backend/.env to override the defaults in app/config.py. Never commit .env.
DATABASE_URL=postgresql+psycopg://effort_user:effort-local@localhost:5444/effort_db
CORS_ORIGINS=["http://localhost:3000"]
# Where the trained arena models are (default: ../ml-engine/models/arena-v1 in this repository)
# MODELS_DIR=C:/path/to/ml-engine/models/arena-v1
# PRELOAD_MODELS=true
